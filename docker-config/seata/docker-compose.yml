version: '3.8'

services:
  seata-server:
    image: seataio/seata-server:1.5.2
    container_name: seata-server
    ports:
      - "8091:8091"
      - "7091:7091"
    environment:
      SEATA_IP: 192.168.2.20
      SEATA_PORT: 8091
    volumes:
      - /var/seata/config:/seata-server/resources
      - /var/seata/sessionStore:/seata-server/sessionStore
    networks:
      - mynet

networks:
  mynet:
    external: true