# ==================== 存储模式配置 ====================
# 指定使用数据库持久化事务数据
store.mode=db
store.db.datasource=druid
store.db.dbType=mysql
store.db.driverClassName=com.mysql.cj.jdbc.Driver
# 替换为你的真实 MySQL 地址和 seata 专属数据库名
store.db.url=jdbc:mysql://192.168.2.20:3306/seata?useUnicode=true&rewriteBatchedStatements=true
store.db.user=root
store.db.password=123456
store.db.minConn=5
store.db.maxConn=30
store.db.globalTable=global_table
store.db.branchTable=branch_table
store.db.lockTable=lock_table
store.db.queryLimit=100
store.db.maxWait=5000

# ==================== 事务分组映射配置 ====================
# 【关键】这里必须配置！格式为 service.vgroupMapping.{你的业务微服务事务组名}=default
# 例如：如果订单服务的 application.yml 中配置了 tx-service-group: order_tx_group
# 那么这里就必须加上：service.vgroupMapping.order_tx_group=default
service.vgroupMapping.default_tx_group=default

# ==================== 集群节点列表配置 ====================
# 指定 Seata Server 的实际 IP 和端口，供客户端进行 RPC 通信

service.default.grouplist=192.168.2.20:8091

# ==================== 其他高级配置(可选) ====================
server.recovery.committingRetryPeriod=1000
server.recovery.asynCommittingRetryPeriod=1000
server.recovery.rollbackingRetryPeriod=1000
server.recovery.timeoutRetryPeriod=1000
server.undo.logSaveDays=7
server.undo.logDeletePeriod=86400000
transport.serialization=seata
transport.compressor=none
metrics.enabled=false